# LLM Agent Sandbox

This directory is reserved for temporary or ad-hoc tests written by AI agents during development and pair-programming sessions.

To prevent agent scratch work from cluttering the repository, all files in this directory are git-ignored by default, with the exception of `.gitignore` and this `README.md`.

Feel free to run and edit files here safely!
